import 'package:flutter/material.dart';

class PartTrackerOverviewScreen extends StatelessWidget {
  const PartTrackerOverviewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: ListView(),
    );
  }
}
